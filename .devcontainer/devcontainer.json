{
	"name": "dotfiles",

	"dockerComposeFile": "../docker-compose.yaml",

	"service": "dotfiles",

    "workspaceFolder": "${localWorkspaceFolder}",

	"settings": {

		// Use login shells by default in the integrated terminal on linux
		"terminal.integrated.shellArgs.linux": ["--login"]
	}
}
