# >>> mise:activate >>> managed by mise — do not edit between markers
eval "$(mise activate bash --shims)"
# <<< mise:activate <<<

{{ template "env" (dict "SHELL" "bash") }}

if [[ $- == *i* ]]; then
  . "$HOME/.bashrc"
fi
