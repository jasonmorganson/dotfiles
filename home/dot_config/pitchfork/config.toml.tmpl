[settings.general]
mise = true

[daemons.symphony-server]
run = "mise run symphony:serve"
dir = "~/.local/share/symphony/elixir"
retry = true
boot_start = true
ready_port = {{ env "SYMPHONY_PORT" | default "4410" }}
