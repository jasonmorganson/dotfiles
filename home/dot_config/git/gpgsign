[gpg]
	format = ssh
	program = ssh-keygen

[gpg "ssh"]
	program = ssh-keygen
	allowedSignersFile = ~/.ssh/allowed_signers
	
[user]
    signingkey = ~/.ssh/id_rsa.pub

[tag]
	gpgSign = true

[commit]
    gpgsign = true
