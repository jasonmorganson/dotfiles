#!/bin/sh
set -eu

mkdir -p "$HOME/.cache/terminfo" "$HOME/.local/share/terminfo"
infocmp -x xterm-ghostty > "$HOME/.cache/terminfo/xterm-ghostty.terminfo" 2>/dev/null || true
find "$HOME/.cache/terminfo" -name '*.terminfo' -type f -exec tic -x -o "$HOME/.local/share/terminfo" {} \;
