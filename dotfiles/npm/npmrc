# XDG-aligned npm config.
# prefix is intentionally omitted here when Node.js is managed by nvm.

cache=${XDG_CACHE_HOME}/npm
init-module=${XDG_CONFIG_HOME}/npm/config/npm-init.js
logs-dir=${XDG_STATE_HOME}/npm/logs
