#!/bin/sh

. ./.profile

{{ if eq .chezmoi.os "linux" }}

set --

{{ if eq .chezmoi.osRelease.id "debian" }}

# Locales
set -- "$@" \
    locales \
    locales-all

sudo apt-get update

sudo apt-get install --yes "$@"

{{ end }}
{{ end }}
