{{- $user := default (promptString "user") (env "USER") -}}
{{- $name := default (promptString "name") (env "NAME") -}}
{{- $email := default (promptString "email") (env "EMAIL") -}}
data:
  user: {{ $user }}
  name: {{ $name }}
  email: {{ $email }}
